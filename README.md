# WB API Formuli Python

Python API для работы с формулами и аналитикой Wildberries

## 🌟 Новое в проекте!

### 🚀 Multi-Report Loader - Параллельная загрузка отчётов

**Загружайте сразу несколько отчётов WB API одновременно!**

```python
from api.multi_report_loader import MultiReportLoader

loader = MultiReportLoader(api_key="ваш_ключ")

# Загрузка 5 отчётов параллельно (2-3 секунды!)
results = loader.load_reports_sync(
    report_keys=["reportDetail", "sales", "orders", "stocks", "balance"],
    date_from="2025-10-13T00:00:00Z",
    date_to="2025-10-19T23:59:59Z"
)

# Сохраняем всё в один JSON
loader.save_to_json(results, "wb_reports.json")
```

**Преимущества:**
- ⚡ **Ускорение 3-5x** - асинхронная загрузка всех отчётов
- 📁 **Единый файл** - все данные в одном JSON
- 🛡️ **Надёжность** - если 1 отчёт упал, остальные загрузятся

**Быстрый тест:**
```bash
python test_multi_reports.py
```

📚 **Подробная документация:** [docs/MULTI_REPORT_LOADER.md](docs/MULTI_REPORT_LOADER.md)

---

## 🎯 Возможности

- 📊 **Отчёт о реализации (v5)** - главный финансовый отчёт со всеми деталями
- ⚙️ Получение данных о продажах через WB API
- 💰 Расчёт финансовых метрик (прибыль, маржа, ROI, COGS)
- ✍️ Ручной ввод недостающих данных (себестоимость, самовыкупы, раздачи)
- 🔄 **Сравнение данных API vs CSV** - автоматическая проверка расхождений
- 💾 Экспорт результатов в JSON и CSV
- 💻 Интерактивный CLI

## 📊 Поддерживаемые метрики

### Объёмы (шт)
- Продажи, доставки, возвраты, отказы
- Процент отказов и возвратов
- Чистые продажи

### Финансовые показатели (руб)
- Выручка (до и после СПП)
- Себестоимость проданного товара (COGS)
- Валовая прибыль
- Чистая прибыль

### Расходы WB (руб)
- Логистика, хранение, приёмка
- Штрафы, доплаты
- Комиссия (с СПП и без)
- Реклама (ДРР)

### KPI
- Маржинальность (%)
- ROI (%)
- Средний чек
- Эффективность по выручке

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/GiornoGiovanaJoJo/wb-api-formuli-python.git
cd wb-api-formuli-python
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

```bash
cp .env.example .env
```

Откройте `.env` и укажите ваш WB API ключ:
```env
WB_API_KEY=ваш_ключ_здесь
```

### 5. Запуск

**Тест мультирепортера:**
```bash
python test_multi_reports.py
```

**Сравнение API и CSV:**
```bash
python compare_api_vs_csv.py
```

**Основной CLI:**
```bash
python run.py
```

**Примеры:**
```bash
python examples/multi_report_example.py
```

---

## 📁 Структура проекта

```
wb-api-formuli-python/
├── api/
│   ├── wb_client.py           # Базовый WB API клиент
│   └── multi_report_loader.py # 🆕 Параллельная загрузка отчётов
├── config/                 # Конфигурация
├── models/                 # Модели данных
├── data/                   # Загрузчики данных
├── analyzer/               # Расчёт метрик
├── storage/                # Экспорт результатов
├── cli/                    # Интерактивный интерфейс
├── tests/                  # Тесты
├── examples/               # 🆕 Примеры использования
├── docs/                   # Документация
│   ├── WB_API_COMPLETE.md  # Полная документация WB API
│   └── MULTI_REPORT_LOADER.md # 🆕 Документация мультирепортера
├── data_samples/           # Примеры данных
├── output/                 # Результаты
├── test_multi_reports.py   # 🆕 Быстрый тест
├── compare_api_vs_csv.py   # 🆕 Сравнение API vs CSV
└── run.py                  # Точка входа
```

---

## 🔑 Получение WB API ключа

1. Перейдите на [seller.wildberries.ru](https://seller.wildberries.ru)
2. **Настройки** → **API ключи**
3. **Создайте новый ключ**
4. **Выберите разрешения:**
   - ☑️ Статистика
   - ☑️ Финансы
   - ☑️ Доставки
   - ☑️ Склады
5. **Скопируйте** ключ в `.env` файл

---

## 📚 Использование

### 🆕 Сравнение данных API vs CSV (Рекомендуется!)

**Автоматически сравните данные из WB API с вашим CSV отчётом:**

```bash
python compare_api_vs_csv.py
```

**Что делает скрипт:**
1. ✅ Загружает данные через WB API (reportDetailByPeriod v5)
2. ✅ Загружает данные из CSV файла
3. ✅ Рассчитывает метрики для обоих источников
4. ✅ Сравнивает результаты и показывает расхождения
5. ✅ Сохраняет отчёт о сравнении в JSON

**Пример вывода:**
```
📊 СРАВНЕНИЕ ДАННЫХ API vs CSV
============================================================

🔢 Общие показатели:

✅ Артикулов:
   API: 5.00
   CSV: 5.00
   Разница: 0.00 (0.0%)

✅ Продаж (шт):
   API: 15.00
   CSV: 14.00
   Разница: +1.00 (+7.1%)

✅ К выплате (руб):
   API: 12345.67
   CSV: 12100.00
   Разница: +245.67 (+2.0%)
```

### 🆕 Multi-Report Loader

```python
from api.multi_report_loader import MultiReportLoader
from pathlib import Path

loader = MultiReportLoader(api_key="ваш_ключ")

# Выбираем нужные отчёты
reports = [
    "reportDetail",  # 📊 Главный финансовый отчёт (v5)
    "sales",         # Продажи
    "orders",        # Заказы
    "stocks",        # Остатки
    "balance"        # Баланс
]

# Загружаем параллельно
results = loader.load_reports_sync(
    report_keys=reports,
    date_from="2025-10-13T00:00:00Z",
    date_to="2025-10-19T23:59:59Z"
)

# Сохраняем
loader.save_to_json(results, Path("output/wb_reports.json"))
loader.print_summary(results)
```

### CLI режим

```bash
python run.py
```

Выберите источник данных:
- WB API (реал-тайм данные)
- CSV отчёт
- JSON файл

### Программное использование

```python
from api.wb_client import WBAPIClient
from analyzer.calculator import Calculator

# Получение данных из API
client = WBAPIClient(api_key="ваш_ключ")

# Главный финансовый отчёт (v5)
report = client.get_report_detail_by_period(
    date_from="2025-10-13",
    date_to="2025-10-19",
    limit=100000
)

# Расчёт метрик
calc = Calculator()
metrics = calc.calculate_product_metrics(
    sales_data=report,
    cost_per_unit=2000,  # ручной ввод
    self_purchase_count=1,
    giveaway_count=0
)

print(f"Чистая прибыль: {metrics['net_profit']:.2f} руб")
print(f"Маржа: {metrics['margin_percent']:.2f}%")
print(f"ROI: {metrics['roi_percent']:.2f}%")
```

---

## 🧪 Тестирование

```bash
# Быстрый тест мультирепортера
python test_multi_reports.py

# Сравнение API и CSV
python compare_api_vs_csv.py

# Unit тесты
python -m pytest tests/
```

---

## 📊 Формулы расчёта

```python
# Себестоимость проданного товара
COGS = (Продажи - Возвраты - Самовыкупы) × Себестоимость_на_единицу

# Валовая прибыль
Гросс_прибыль = Выручка_после_СПП - COGS

# Все расходы WB
Расходы = Логистика + Хранение + Штрафы + Приёмка + Комиссия + ДРР

# Чистая прибыль
Чистая_прибыль = Гросс_прибыль - Расходы

# Маржинальность
Маржа_% = (Гросс_прибыль / Выручка) × 100

# ROI
ROI_% = (Чистая_прибыль / COGS) × 100
```

---

## 📝 Примеры данных

В папке `data_samples/` находятся примеры:
- `43-nedelia-2-List1.csv` - WB отчёт по товарам
- `test.txt` - данные о тарифах хранения
- `README.md` - описание структуры CSV

---

## 🔗 Полезные ссылки

- 📚 [Официальная документация WB API](https://dev.wildberries.ru/openapi/reports)
- 📊 [Финансовые отчёты](https://dev.wildberries.ru/openapi/financial-reports-and-accounting)
- 📦 [Панель продавца](https://seller.wildberries.ru)
- 🔑 [Получение API ключей](https://seller.wildberries.ru/settings/api-keys)

---

## 🤝 Вклад в проект

Приветствуются pull requests! Для крупных изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 📧 Контакты

Если у вас есть вопросы, создайте issue в репозитории.