# 🔌 WB API - ПОЛНАЯ ДОКУМЕНТАЦИЯ (18.11.2025)

## 📍 Базовая информация

**Базовый URL:** `https://statistics-api.wildberries.ru`

**Версия API:** `v1` (Важно! v5 не существует)

**Авторизация:** 
Прямая передача API ключа в заголовке
```
Authorization: YOUR_API_KEY
Content-Type: application/json
```

**Официальная документация:** https://dev.wildberries.ru/openapi/reports

---

## 📊 ОСНОВНЫЕ ОТЧЁТЫ (СТАТИСТИКА)

### 1️⃣ ПОСТАВКИ

**Endpoint:** `/api/v1/supplier/incomes`

**Метод:** GET

**Описание:** Количество поставок товаров на склады WB

**Обновление:** Раз в 30 минут

**Лимит:** 100,000 строк на запрос

**Параметры:**
- `dateFrom` (required) - Дата и время последнего изменения (RFC3339)

**Пример:**
```python
from api.wb_client import WBAPIClient

client = WBAPIClient(api_key="your_key")
incomes = client.get_incomes(date_from="2025-10-13T00:00:00Z")
print(f"Получено {len(incomes)} поставок")
```

---

### 2️⃣ СКЛАДЫ

**Endpoint:** `/api/v1/supplier/stocks`

**Метод:** GET

**Описание:** Остатки товаров на складах WB

**Обновление:** Раз в 30 минут

**Лимит:** 60,000 строк

**Пример:**
```python
stocks = client.get_stocks(date_from="2025-10-13T00:00:00Z")
for stock in stocks:
    print(f"nm_id: {stock['nmId']}, количество: {stock['quantity']}")
```

---

### 3️⃣ ЗАКАЗЫ

**Endpoint:** `/api/v1/supplier/orders`

**Метод:** GET

**Описание:** Информация обо всех заказах

**Обновление:** Раз в 30 минут

**Лимит:** 80,000 строк

**Хранение:** 90 дней

**Параметры:**
- `dateFrom` (required) - Дата последнего изменения
- `flag` (optional) - 0 (все) или 1 (только новые)

**Пример:**
```python
orders = client.get_orders(
    date_from="2025-10-13T00:00:00Z",
    flag=0  # все заказы
)
```

**ВАЖНО:**
- 1 строка = 1 заказ = 1 сборочное задание = 1 единица товара
- Используйте поле `srid` для определения заказа

---

### 4️⃣ ПРОДАЖИ (ОСНОВНОЙ ENDPOINT!)

**Endpoint:** `/api/v1/supplier/sales`

**Метод:** GET

**Описание:** Информация о продажах и возвратах

**Обновление:** Раз в 30 минут

**Лимит:** 80,000 строк

**Хранение:** 90 дней

**Пример:**
```python
sales = client.get_sales(date_from="2025-10-13T00:00:00Z")
for sale in sales:
    print(f"nm_id: {sale['nmId']}, к выплате: {sale['forPay']}")
```

**КЛЮЧЕВЫЕ ПОЛЯ ОТВЕТА:**
```json
{
  "nmId": 1234567,
  "totalPrice": 1887,          // Цена до скидки
  "discountPercent": 18,        // Скидка
  "spp": 20,                    // СПП (скидка постоянного покупателя)
  "forPay": 1284.01,            // К выплате продавцу
  "finishedPrice": 1145,        // Цена для покупателя
  "priceWithDisc": 1547,        // Цена со скидкой (до СПП)
  "paymentSaleAmount": 93,      // Сумма оплаты с WB Кошелька
  "saleID": "S9993700024",      // ID продажи
  "srid": "11.rf9ef...",        // Уникальный ID заказа
}
```

---

## 💰 ФИНАНСОВЫЕ ОТЧЁТЫ

### 🌍 ПРОДАЖИ ПО РЕГИОНАМ

**Endpoint:** `/api/v1/analytics/region-sale`

**Максимальный период:** 31 день

**Пример:**
```python
region_sales = client.get_region_sales(
    date_from="2025-10-13",
    date_to="2025-10-19"
)
```

### 🏷️ МАРКИРОВАННЫЕ ТОВАРЫ

**Endpoint:** `/api/v1/analytics/excise-report`

**Пример:**
```python
excise = client.get_excise_report(
    date_from="2025-10-13",
    date_to="2025-10-19",
    countries=["RU", "KZ", "BY"]
)
```

---

## ⚠️ ОТЧЁТЫ ОБ УДЕРЖАНИЯХ (ШТРАФЫ)

### 📦 ЗАНИЖЕНИЕ ГАБАРИТОВ

**Endpoint:** `/api/v1/analytics/warehouse-measurements`

**Пример:**
```python
penalties = client.get_warehouse_measurements(
    date_from="2025-10-13T00:00:00Z",
    date_to="2025-10-19T23:59:59Z",
    tab="penalty",  # или "measurement"
    limit=100
)
```

### 🚫 САМОВЫКУПЫ (30% стоимости)

**Endpoint:** `/api/v1/analytics/antifraud-details`

**Отчёт:** Каждую среду до 7:00 МСК за неделю

**Данные с:** Август 2023

**Пример:**
```python
antifraud = client.get_antifraud_details(date="2025-10-15")
```

### ❌ ПОДМЕНА ТОВАРА (100% стоимости)

**Endpoint:** `/api/v1/analytics/incorrect-attachments`

**Макс период:** 31 день | **Данные с:** Июнь 2023

**Пример:**
```python
incorrect = client.get_incorrect_attachments(
    date_from="2025-10-13",
    date_to="2025-10-19"
)
```

### 🏷️ МАРКИРОВКА ТОВАРА

**Endpoint:** `/api/v1/analytics/goods-labeling`

**Макс период:** 31 день | **Данные с:** Март 2024

### ♻️ СМЕНА ХАРАКТЕРИСТИК

**Endpoint:** `/api/v1/analytics/characteristics-change`

**Макс период:** 31 день | **Данные с:** 28 декабря 2021

---

## 🔑 ПОЛУЧЕНИЕ API КЛЮЧА

1. **Перейдите** на [seller.wildberries.ru](https://seller.wildberries.ru)
2. **Настройки** → **API ключи**
3. **Создать новый ключ**
4. **Выберите разрешения:**
   - ☑️ Статистика
   - ☑️ Финансы
   - ☑️ Доставки
   - ☑️ Склады
5. **Скопировать** ключ
6. **Вставить** в `.env`:

```env
WB_API_KEY=ваш_ключ_здесь
```

---

## 🔄 ПАГИНАЦИЯ

Для получения всех данных используйте `lastChangeDate` из последней строки:

```python
def get_all_sales(client, start_date):
    """Получить все продажи с автоматической пагинацией."""
    all_sales = []
    date_from = start_date
    
    while True:
        sales = client.get_sales(date_from=date_from)
        
        if not sales or len(sales) == 0:
            break  # Нет больше данных
        
        all_sales.extend(sales)
        
        # Используем lastChangeDate для следующего запроса
        date_from = sales[-1]['lastChangeDate']
        
        print(f"Загружено {len(all_sales)} продаж...")
    
    return all_sales

# Использование
sales = get_all_sales(client, "2025-10-13T00:00:00Z")
print(f"Всего загружено: {len(sales)} продаж")
```

---

## ⚡ RATE LIMITS

| Endpoint | Лимит | Интервал |
|----------|-------|----------|
| `/api/v1/supplier/incomes` | 1 запрос | 1 минута |
| `/api/v1/supplier/stocks` | 1 запрос | 1 минута |
| `/api/v1/supplier/orders` | 1 запрос | 1 минута |
| `/api/v1/supplier/sales` | 1 запрос | 1 минута |
| `/api/v1/analytics/*` | 1-10 запросов | 1 минута |

---

## 🛡️ ОБРАБОТКА ОШИБОК

```python
try:
    sales = client.get_sales("2025-10-13T00:00:00Z")
except Exception as e:
    error_msg = str(e)
    
    if "401" in error_msg:
        print("❌ 401: Неверный API ключ")
        print("💡 Проверьте WB_API_KEY в .env файле")
    elif "403" in error_msg:
        print("❌ 403: Нет доступа к этому ресурсу")
        print("💡 Убедитесь, что у API ключа есть нужные разрешения")
    elif "429" in error_msg:
        print("❌ 429: Превышен лимит запросов")
        print("💡 Подождите 1 минуту перед следующим запросом")
    else:
        print(f"❌ Ошибка: {e}")
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- [📚 Официальная документация WB API](https://dev.wildberries.ru/openapi/reports)
- [📦 Панель продавца](https://seller.wildberries.ru)
- [🔑 Получение API ключей](https://seller.wildberries.ru/settings/api-keys)
- [📑 GitHub репозиторий проекта](https://github.com/GiornoGiovanaJoJo/wb-api-formuli-python)