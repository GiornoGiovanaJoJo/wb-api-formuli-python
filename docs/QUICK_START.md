# 🚀 Быстрый старт

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/GiornoGiovanaJoJo/wb-api-formuli-python.git
cd wb-api-formuli-python
```

## Шаг 2: Создание виртуального окружения

### Linux/Mac:
```bash
python3 -m venv venv
source venv/bin/activate
```

### Windows:
```cmd
python -m venv venv
venv\Scripts\activate
```

## Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 4: Настройка конфигурации

```bash
cp .env.example .env
```

Откройте `.env` и укажите ваш API ключ:

```env
WB_API_KEY=ваш_ключ_здесь
```

## Шаг 5: Запуск

```bash
python run.py
```

## 🎯 Что делать дальше?

1. **Выберите источник данных:**
   - WB API (реал-тайм)
   - CSV отчёт
   - JSON файл

2. **Введите ручные данные:**
   - Себестоимость товара
   - Самовыкупы
   - Раздачи
   - Доп. маркетинг

3. **Получите результаты:**
   - JSON отчёт в `output/report.json`
   - CSV отчёт в `output/report.csv`
   - Сводка в консоли

## 🔑 Получение WB API ключа

1. Перейдите на [seller.wildberries.ru](https://seller.wildberries.ru)
2. **Настройки** → **API ключи**
3. **Создать новый ключ**
4. Выберите разрешения:
   - ☑️ Статистика
   - ☑️ Финансы
   - ☑️ Доставки
   - ☑️ Склады
5. **Скопировать** ключ
6. **Вставить** в `.env` файл

## ❓ Проблемы?

Смотрите полную документацию в `README.md`